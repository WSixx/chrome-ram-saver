{
  "name": "ram-saver",
  "version": "1.10.1",
  "description": "Chrome extension for RAM saving via tab suspension",
  "private": true,
  "scripts": {
    "test": "jest",
    "test:unit": "jest tests/unit --verbose",
    "test:e2e": "playwright test",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "lint": "eslint ."
  },
  "devDependencies": {
    "jest": "^30.5.1",
    "@playwright/test": "^1.47.0",
    "eslint": "^10.10.0",
    "globals": "^16.0.0"
  }
}
